#include "main.h"
#include <string.h>
/**
 * rev_string - reverses a string
 * @s: string in question
 *
 * Return: void
 */
void rev_string( char *s)
{
	int i;
	int j = strlen(s);

	for (i = j; i >= 0; i--)
	{
	;
	}
}
